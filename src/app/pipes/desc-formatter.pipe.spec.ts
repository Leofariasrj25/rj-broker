import { DescFormatterPipe } from './desc-formatter.pipe';

describe('DescFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new DescFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
